{
  "name": "buzz-relay-dm-bridge",
  "version": "0.1.0",
  "description": "Bridge one relay channel to one direct-message thread on a second relay, using a single identity on both.",
  "license": "MIT",
  "type": "module",
  "bin": { "buzz-relay-dm-bridge": "src/index.mjs" },
  "main": "src/index.mjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.mjs",
    "set-profile": "node src/set-profile.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "nostr-tools": "^2.25.2",
    "ws": "^8.18.0"
  }
}
